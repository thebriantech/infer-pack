"""Internal utility functions shared across stage implementations.

These are NOT ``PipelineStage`` classes — they are pure helper functions
used by multiple stage modules to avoid code duplication.
"""
